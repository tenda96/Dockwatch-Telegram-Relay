# Dockwatch-Telegram-Relay
A small Docker relay that receives **Mattermost webhook notifications from Dockwatch** and forwards them to a **Telegram group or Telegram forum topic**.  This is useful when Dockwatch does not support Telegram `message_thread_id` directly.
